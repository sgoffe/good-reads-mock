<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Book List</title>
    <style>
        .book-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
        }
        .book-card {
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 10px;
            width: 200px;
            text-align: center;
        }
        .book-image {
            width: 100%;
            height: auto;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <h1>Book List</h1>
    
    <div class="book-container">
        <% @books.each do |book| %>
            <div class="book-card">
                <% if book.images.attached? %>
                    <%= image_tag book.images.first.variant(resize_to_limit: [150, 150]), class: "book-image", alt: "#{book.title} cover" %>
                <% else %>
                    <%= image_tag 'placeholder.jpg', class: "book-image", alt: "Placeholder image" %>
                <% end %>
                <h3><%= book.title %></h3>
                <p><strong>Author:</strong> <%= book.author %></p>
            </div>
        <% end %>
    </div>
</body>
</html>

<%= form_with url: url, method: :get, local: true, class: "flex items-center" do |form| %>
  <%= form.text_field :query, value: query_value, class: "w-full bg-transparent placeholder:text-slate-400 text-slate-700 text-sm border border-slate-200 rounded-full px-3 py-2 transition duration-300 ease #{size_class}", placeholder: placeholder %>
<% end %>
